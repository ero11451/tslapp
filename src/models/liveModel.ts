export interface Live {
  expand: boolean;
  title;
  description;
  time;
}

export interface LiveSettings {
  status: boolean;
  title: string;
  link: string;
  isActive: boolean;
  nextEventBanner: string;
}
